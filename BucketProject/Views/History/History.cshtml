@using BucketProject.BLL.Business_Logic.Entity
@using BucketProject.DAL.Models.Enums

@{
    var grouped = ViewBag.GroupedGoals as Dictionary<Category, Dictionary<string, Dictionary<GoalType, List<Goal>>>>;
    var username = ViewBag.Username as string;
}

<div class="container mt-4">

    @if (grouped != null && grouped.Any())
    {
        foreach (var categoryGroup in grouped)
        {
            <h3 class="mt-5">@categoryGroup.Key.ToString() Goals</h3>

            foreach (var timeGroup in categoryGroup.Value.OrderByDescending(g => g.Key))
            {
                <h4 class="mt-3">@timeGroup.Key</h4>

                foreach (var typeGroup in timeGroup.Value)
                {
                    <h5>@typeGroup.Key</h5>
                    <ul class="list-group mb-3">
                        @foreach (var goal in typeGroup.Value)
                        {
                            <li class="list-group-item">
                                <div class="d-flex justify-content-between">
                                    <div>
                                        <strong>@goal.Description</strong><br />
                                        <small class="text-muted">Deadline: @goal.Deadline?.ToShortDateString()</small>
                                    </div>
                                    <div class="text-end">
                                        @if (goal.IsDone)
                                        {
                                            <span class="badge bg-success">Completed</span>

                                            <br />
                                            <small class="text-muted">on @goal.CompletedAt?.ToShortDateString()</small>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">Not Completed</span>
                                        }
                                    </div>
                                </div>
                            </li>
                        }
                    </ul>
                }
            }
        }
    }
    else
    {
        <p class="text-muted">No expired goals found.</p>
    }
</div>
