@model SocialViewModel


@if (!Model.Friends.Any() &&
           !Model.IncomingRequests.Any() &&
           !Model.PotentialFriends.Any())
{
    <div class="alert alert-warning">
       No users found.
    </div>
}
@if (Model.Friends.Any())
{
    <h4>Matching Friends</h4>
    <ul class="list-group mb-3">
        @foreach (var f in Model.Friends)
        {
            var imgSrc = (f.Picture != null && f.Picture.Length > 0)
            ? $"data:image/jpeg;base64,{Convert.ToBase64String(f.Picture)}"
            : null;

            <li class="list-group-item d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    @if (imgSrc != null)
                    {
                        <img src="@imgSrc" alt="User avatar" class="rounded-circle me-2" style="width:32px; height:32px; object-fit: cover;" />
                    }
                    else
                    {
                        <div class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center me-2"
                             style="width:32px; height:32px; font-size:0.9rem; font-weight:bold;">
                            @char.ToUpperInvariant(f.Username[0])
                        </div>
                    }
                    <span>@f.Username</span>
                </div>
                <form asp-action="RemoveFriend" method="post">
                    <input type="hidden" name="friendId" value="@f.Id" />
                    <button class="btn btn-sm btn-danger">Remove</button>
                </form>
            </li>
        }
    </ul>
}

@if (Model.IncomingRequests.Any())
{
    <h4>Matching Incoming Requests</h4>
    <ul class="list-group mb-3">
        @foreach (var r in Model.IncomingRequests)
        {
            var imgSrc = (r.Picture != null && r.Picture.Length > 0)
            ? $"data:image/jpeg;base64,{Convert.ToBase64String(r.Picture)}"
            : null;

            <li class="list-group-item d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    @if (imgSrc != null)
                    {
                        <img src="@imgSrc" class="rounded-circle me-2" style="width:32px; height:32px; object-fit: cover;" />
                    }
                    else
                    {
                        <div class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center me-2"
                             style="width:32px; height:32px; font-size:0.9rem; font-weight:bold;">
                            @char.ToUpperInvariant(r.Username[0])
                        </div>
                    }
                    <span>@r.Username</span>
                </div>
                <div class="btn-group">
                    <form asp-action="AcceptFriendRequest" method="post">
                        <input type="hidden" name="requesterId" value="@r.Id" />
                        <button class="btn btn-sm btn-success">Accept</button>
                    </form>
                    <form asp-action="DeclineFriendRequest" method="post" class="ms-2">
                        <input type="hidden" name="requesterId" value="@r.Id" />
                        <button class="btn btn-sm btn-danger">Decline</button>
                    </form>
                </div>
            </li>
        }
    </ul>
}

@if (Model.PotentialFriends.Any())
{
    <h4>People You May Know</h4>
    <ul class="list-group mb-3">
        @foreach (var p in Model.PotentialFriends)
        {
            var imgSrc = (p.Picture != null && p.Picture.Length > 0)
            ? $"data:image/jpeg;base64,{Convert.ToBase64String(p.Picture)}"
            : null;

            bool isPending = Model.OutgoingRequestIds.Contains(p.Id);

            <li class="list-group-item d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center">
                    @if (imgSrc != null)
                    {
                        <img src="@imgSrc" class="rounded-circle me-2" style="width:32px; height:32px; object-fit: cover;" />
                    }
                    else
                    {
                        <div class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center me-2"
                             style="width:32px; height:32px; font-size:0.9rem; font-weight:bold;">
                            @char.ToUpperInvariant(p.Username[0])
                        </div>
                    }
                    <span>@p.Username</span>
                </div>
                @if (!isPending)
                {
                    <form asp-action="SendFriendRequest" method="post">
                        <input type="hidden" name="friendId" value="@p.Id" />
                        <button class="btn btn-sm btn-primary">Add Friend</button>
                    </form>
                }
                else
                {
                    <form asp-action="RemoveFriend" method="post">
                        <input type="hidden" name="friendId" value="@p.Id" />
                        <button class="btn btn-sm btn-warning">Cancel Request</button>
                    </form>
                }
            </li>
        }
    </ul>

    @if ((int)ViewBag.TotalPages > 1)
    {
        <div class="pagination mt-3 d-flex justify-content-center gap-2">
            @if ((int)ViewBag.CurrentPage > 1)
            {
                <a href="javascript:void(0);" class="page-link" data-page="@(ViewBag.CurrentPage - 1)" data-search="@ViewBag.SearchTerm">« Previous</a>
            }

            @for (int i = 1; i <= (int)ViewBag.TotalPages; i++)
            {
                var activeClass = i == (int)ViewBag.CurrentPage ? "active-page" : "";
                <a href="javascript:void(0);" class="page-link @activeClass" data-page="@i" data-search="@ViewBag.SearchTerm">@i</a>
            }

            @if ((int)ViewBag.CurrentPage < (int)ViewBag.TotalPages)
            {
                <a href="javascript:void(0);" class="page-link" data-page="@(ViewBag.CurrentPage + 1)" data-search="@ViewBag.SearchTerm">Next »</a>
            }
        </div>
    }
}

